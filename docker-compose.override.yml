services:
  postgres:
    ports:
      - "${POSTGRES_EXTERNAL_PORT:-15432}:5432"

  backend:
    ports:
      - "${BACKEND_EXTERNAL_PORT:-8001}:8000"

  nginx:
    ports:
      - "${NGINX_EXTERNAL_PORT:-8080}:80"
